import { Routes } from '@angular/router';
import { InstituteSectorsComponent } from './institute-sectors/institute-sectors.component';

export const routes: Routes = [
    {
        path : 'sectors', 
        component : InstituteSectorsComponent
    }
];
